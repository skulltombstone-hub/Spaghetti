<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.ButterscotchAndroid" parent="android:Theme.Material.NoActionBar">
        <!-- Use Spaghetti background so cold starts / transitions match Compose dark theme -->
        <item name="android:windowBackground">@color/background</item>
    </style>

    <style name="Theme.ButterscotchAndroid.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.ButterscotchAndroid</item>
    </style>
</resources>
